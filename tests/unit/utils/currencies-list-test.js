import currenciesList from '../../../utils/currencies-list';
import { module, test } from 'qunit';

module('Unit | Utility | currencies list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currenciesList();
  assert.ok(result);
});
